mcp>=1.0.0
websockets>=12.0
dateparser>=1.2.0

# Optional — only needed if ENABLE_SCHEDULER=1
# apscheduler>=3.11.0

# Optional — only needed if ENABLE_PC_CONTROL=1
# pyautogui>=0.9.54
# pyperclip>=1.8.2

# Optional — only needed if ENABLE_BROWSER_PLAYWRIGHT=1
# playwright>=1.50.0
# (also run: python -m playwright install chromium)
